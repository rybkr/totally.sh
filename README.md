# Totally.sh

`totally` is a local CLI for understanding agent sessions: what you worked on,
how much it used and cost, and where the underlying transcript lives.
`totally.sh` is the website that makes the tool public.

## Development

The site uses Hugo, a Markdown-first static-site generator that produces static
HTML with no client-side runtime. Run `hugo server` to develop locally.
